/**
 * Subtemplate: cta-final
 *
 * Estado: STUB (Fase 0). Implementação na Fase 2.
 */

import type { SubtemplateModule } from './types'

export const ctaFinalModule: SubtemplateModule = {
  config: {
    id: 'cta-final',
    textSlots: [],
    imageSlots: [],
    density: 'sparse',
    compatibleBackgrounds: ['*'],
  },
  render: () => {
    throw new Error('[T2] subtemplate cta-final — Fase 2 não implementada')
  },
}

export default ctaFinalModule
